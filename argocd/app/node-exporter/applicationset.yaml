---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: node-exporter
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - 'missingkey=error'
  generators:
    - clusters:
        selector:
          matchLabels:
            cluster-environment: prod
            argocd.argoproj.io/secret-type: cluster
        values:
          namespace: node-exporter
          chartVersion: ">= 4.47.0"
  template:
    metadata:
      name: 'node-exporter-{{.name | slugify}}'
      labels:
        app.kubernetes.io/component: argocd-application
        app.kubernetes.io/part-of: '{{ index .metadata.labels "cluster-environment" }}'
        homelab.wojoinc.xyz/app-group: 'monitoring'
    spec:
      project: default
      sources:
        - repoURL: https://prometheus-community.github.io/helm-charts
          targetRevision: '{{.values.chartVersion}}'
          chart: prometheus-node-exporter
          helm:
            releaseName: "node-exporter"
            skipCrds: true
            valueFiles:
              - $values/argocd/app/node-exporter/values/base/values.yaml
              - $values/argocd/app/node-exporter/values/flavor/{{index .metadata.labels "cluster-flavor"}}/values.yaml
              - $values/argocd/app/node-exporter/values/env/{{index .metadata.labels "cluster-environment"}}/values.yaml
              - $values/argocd/app/node-exporter/values/cluster/{{.name}}/values.yaml
            ignoreMissingValueFiles: true
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          ref: values
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          path: argocd/app/node-exporter/base
          kustomize:
            commonAnnotations:
              argocd.argoproj.io/sync-wave: "0"
            components:
              - ../../../flavor/{{index .metadata.labels "cluster-flavor"}}/common
              - ../components/flavor/{{index .metadata.labels "cluster-flavor"}}
              - ../../../env/{{index .metadata.labels "cluster-environment"}}/common
              - ../components/env/{{index .metadata.labels "cluster-environment"}}
              - ../../../cluster/{{.name}}/common
              - ../components/cluster/{{.name}}
            ignoreMissingComponents: true
      destination:
        server: '{{.server}}'
        namespace: '{{.values.namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
      ignoreDifferences: []
