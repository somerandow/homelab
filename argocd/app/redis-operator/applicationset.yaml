---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: redis-operator
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - 'missingkey=error'
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
        values:
          # renovate: datasource=github-releases depName=ot-container-kit/redis-operator
          targetRevision: "~0.25.0"
          namespace: redis-operator
  template:
    metadata:
      name: 'redis-operator-{{.name | slugify}}'
      labels:
        app.kubernetes.io/component: argocd-application
        homelab.wojoinc.xyz/app-group: 'operators'
    spec:
      project: default
      sources:
        - repoURL: https://ot-container-kit.github.io/helm-charts
          targetRevision: '{{.values.targetRevision}}'
          chart: redis-operator
          helm:
            releaseName: redis-operator
            valueFiles:
              - $values/argocd/app/redis-operator/values/base/values.yaml
              - $values/argocd/app/redis-operator/values/flavor/{{index .metadata.labels "cluster-flavor"}}/values.yaml
              - $values/argocd/app/redis-operator/values/env/{{index .metadata.labels "cluster-environment"}}/values.yaml
              - $values/argocd/app/redis-operator/values/cluster/{{.name}}/values.yaml
            skipCrds: false
            ignoreMissingValueFiles: true
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          ref: values
      destination:
        server: '{{.server}}'
        namespace: '{{.values.namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
      ignoreDifferences: []
