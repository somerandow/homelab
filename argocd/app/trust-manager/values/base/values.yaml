secretTargets:
  enabled: true
  authorizedSecretsAll: false
  authorizedSecrets:
    - "homelab-ca-bundle"
    - "homelab-ca-secret"
    - "ca-secret"
tolerations:
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
