---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: kaneo-db
spec:
  kubernetes:
    audiences: []
    role: "TO_BE_FILLED"
    serviceAccount: kaneo-db
    tokenExpirationSeconds: 600
  method: kubernetes
  mount: homelab
  vaultConnectionRef: vault-secrets-operator/default
