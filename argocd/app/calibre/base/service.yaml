---
apiVersion: v1
kind: Service
metadata:
  name: calibre
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: content-server
      protocol: TCP
      name: content-server
  selector: {}
