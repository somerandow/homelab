---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: grafana-operator
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - 'missingkey=error'
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
        values:
          namespace: grafana-operator
          # renovate: datasource=github-releases depName=grafana/grafana-operator
          targetRevision: "5.22.2"
  template:
    metadata:
      name: 'grafana-operator-{{.name | slugify}}'
      labels:
        app.kubernetes.io/component: argocd-application
        homelab.wojoinc.xyz/app-group: 'operators'
    spec:
      project: default
      sources:
        - repoURL: ghcr.io/grafana/helm-charts
          targetRevision: '{{.values.targetRevision}}'
          chart: grafana-operator
          helm:
            releaseName: grafana-operator
            valueFiles:
              - $values/argocd/app/grafana-operator/values/base/values.yaml
              - $values/argocd/app/grafana-operator/values/flavor/{{index .metadata.labels "cluster-flavor"}}/values.yaml
              - $values/argocd/app/grafana-operator/values/env/{{index .metadata.labels "cluster-environment"}}/values.yaml
              - $values/argocd/app/grafana-operator/values/cluster/{{.name}}/values.yaml
            skipCrds: false
            ignoreMissingValueFiles: true
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          ref: values
      destination:
        server: '{{.server}}'
        namespace: '{{.values.namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
      ignoreDifferences: []
