---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vaultwarden
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - 'missingkey=error'
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
        values:
          # renovate: datasource=docker depName=ghcr.io/somerandow/charts/vaultwarden
          targetRevision: "1.8.1"
          namespace: vaultwarden
  template:
    metadata:
      name: 'vaultwarden-{{.name | slugify}}'
      labels:
        app.kubernetes.io/component: argocd-application
        homelab.wojoinc.xyz/app-group: 'sec'
    spec:
      project: default
      sources:
        - repoURL: ghcr.io/somerandow/charts
          targetRevision: '{{.values.targetRevision}}'
          chart: vaultwarden
          helm:
            releaseName: vaultwarden
            valueFiles:
              - $values/argocd/app/vaultwarden/values/base/values.yaml
              - $values/argocd/app/vaultwarden/values/flavor/{{index .metadata.labels "cluster-flavor"}}/values.yaml
              - $values/argocd/app/vaultwarden/values/env/{{index .metadata.labels "cluster-environment"}}/values.yaml
              - $values/argocd/app/vaultwarden/values/cluster/{{.name}}/values.yaml
            skipCrds: false
            ignoreMissingValueFiles: true
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          ref: values
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          path: argocd/app/vaultwarden/base
          kustomize:
            components:
              - ../../../flavor/{{index .metadata.labels "cluster-flavor"}}
              - ../flavor/{{index .metadata.labels "cluster-flavor"}}/vaultwarden
              - ../../../env/{{index .metadata.labels "cluster-environment"}}
              - ../env/{{index .metadata.labels "cluster-environment"}}/vaultwarden
              - ../../../cluster/{{.name}}
              - ../cluster/{{.name}}/vaultwarden
            ignoreMissingComponents: true
      destination:
        server: '{{ .server }}'
        namespace: '{{ .values.namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: false
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
      ignoreDifferences: []
