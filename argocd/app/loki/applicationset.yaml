---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: loki
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - 'missingkey=error'
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
        values:
          # renovate: datasource=docker depName=ghcr.io/grafana/helm-charts/loki
          targetRevision: "6.55.0"
          namespace: loki
  template:
    metadata:
      name: 'loki-{{.name | slugify}}'
      labels:
        app.kubernetes.io/component: argocd-application
        homelab.wojoinc.xyz/app-group: 'monitoring'
    spec:
      project: default
      sources:
        - repoURL: ghcr.io/grafana/helm-charts
          targetRevision: '{{.values.targetRevision}}'
          chart: loki
          helm:
            releaseName: loki
            valueFiles:
              - $values/argocd/app/loki/values/base/values.yaml
              - $values/argocd/app/loki/values/flavor/{{index .metadata.labels "cluster-flavor"}}/values.yaml
              - $values/argocd/app/loki/values/env/{{index .metadata.labels "cluster-environment"}}/values.yaml
              - $values/argocd/app/loki/values/cluster/{{.name}}/values.yaml
            skipCrds: false
            ignoreMissingValueFiles: true
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          ref: values
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          path: argocd/app/loki/base
          kustomize:
            components:
              - ../../../flavor/{{index .metadata.labels "cluster-flavor"}}
              - ../flavor/{{index .metadata.labels "cluster-flavor"}}/loki
              - ../../../env/{{index .metadata.labels "cluster-environment"}}
              - ../env/{{index .metadata.labels "cluster-environment"}}/loki
              - ../../../cluster/{{.name}}
              - ../cluster/{{.name}}/loki
            ignoreMissingComponents: true
      destination:
        server: '{{ .server }}'
        namespace: '{{ .values.namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
      ignoreDifferences: []
