---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: renovate
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - 'missingkey=error'
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
        values:
          chartVersion: "43.20.2"
          namespace: renovate
  template:
    metadata:
      name: 'renovate-{{.name | slugify}}'
      labels:
        app.kubernetes.io/component: argocd-application
        homelab.wojoinc.xyz/app-group: 'sec'
    spec:
      project: default
      sources:
        - repoURL: ghcr.io/renovatebot/charts
          targetRevision: '{{.values.chartVersion}}'
          chart: renovate
          helm:
            releaseName: renovate
            valueFiles:
              - $values/argocd/app/renovate/values/base/values.yaml
              - $values/argocd/app/renovate/values/flavor/{{index .metadata.labels "cluster-flavor"}}/values.yaml
              - $values/argocd/app/renovate/values/env/{{index .metadata.labels "cluster-environment"}}/values.yaml
              - $values/argocd/app/renovate/values/cluster/{{.name}}/values.yaml
            skipCrds: false
            ignoreMissingValueFiles: true
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          ref: values
        - repoURL: 'https://github.com/somerandow/homelab'
          targetRevision: main
          path: argocd/app/renovate/base
          kustomize:
            components:
              - ../../../flavor/{{index .metadata.labels "cluster-flavor"}}/common
              - ../flavor/{{index .metadata.labels "cluster-flavor"}}/renovate
              - ../../../env/{{index .metadata.labels "cluster-environment"}}/common
              - ../env/{{index .metadata.labels "cluster-environment"}}/renovate
              - ../../../cluster/{{.name}}/common
              - ../cluster/{{.name}}/renovate
            ignoreMissingComponents: true
      destination:
        server: '{{ .server }}'
        namespace: '{{ .values.namespace }}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: false
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
      ignoreDifferences: []
